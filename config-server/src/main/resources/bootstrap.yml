# Bootstrap is loaded with Spring parent context; before application.yml.
spring:
  application:
    name: configserver
    # Set up using absolute path of config-server-repository on local machine
  cloud:
    config:
      server:
        git:
          uri: file:/home/kali/IdeaProjects/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true